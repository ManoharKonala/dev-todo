apiVersion: v1
kind: Service
metadata:
  name: todoflow-svc
  labels:
    app: todoflow
spec:
  type: NodePort
  selector:
    app: todoflow
    deploy_color: blue
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
